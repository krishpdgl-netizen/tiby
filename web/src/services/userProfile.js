// Legacy file — kept for import compatibility only.
// All user data now lives in Neon PostgreSQL via the backend API.

export async function getUserContext() { return {} }
export function clearUserContextCache() {}
